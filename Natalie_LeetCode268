class Solution {
    public int missingNumber(int[] nums) {
        int sum = 0, orisum = ((nums.length+1)*nums.length)/2;
        for(int i = 0;i<nums.length;i++){
            sum += nums[i];
        }
        return orisum-sum;
    }
}
